| Patch file                                                                 | Description                                | Upstream status | Link                                                                                                                                                                                                         |
|----------------------------------------------------------------------------|--------------------------------------------|-----------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `0001-net-macb-flush-PCIe-posted-write-after-TSTART-doorbe.patch`           | macb: flush PCIe posted write after TSTART doorbell — gated behind new `MACB_CAPS_PCIE_POSTED_WRITES` cap (RP1-only) so SoC-integrated variants don't pay the readback cost | v2 submitted to netdev | [v2 thread](https://lore.kernel.org/netdev/20260514215459.36109-1-lukasz@raczylo.com/T/)                                                                                                                |
| `0002-net-macb-insert-PCIe-read-barrier-before-TX-completi.patch`           | macb: insert non-destructive PCIe read barrier (`queue_readl(queue, IMR)`) before `macb_tx_complete_pending()` in `macb_tx_poll()`. Replaces the v1 ISR-read form which was destructive on read-clear silicon (RP1) — that read silently consumed RCOMP / ROVR / TXUBR bits, causing silent RX-completion loss at moderate-to-heavy load | v2 submitted to netdev | [v2 thread](https://lore.kernel.org/netdev/20260514215459.36109-1-lukasz@raczylo.com/T/)                                                                                                                |
| `0003-net-macb-add-TX-stall-watchdog-to-recover-from-lost-.patch`           | macb: per-queue `delayed_work` watchdog that calls `macb_tx_restart()` if tx_tail hasn't advanced. v2 uses a `bool tx_stall_tail_moved` flag (pelwell-suggested form) instead of a tx_tail snapshot, gates the check on `netif_carrier_ok()` to eliminate a boot-time false positive, and wraps the stall-warn in `if (printk_ratelimit()) netdev_warn(...)` so events stay observable while bounded | v2 submitted to netdev | [v2 thread](https://lore.kernel.org/netdev/20260514215459.36109-1-lukasz@raczylo.com/T/) · [v2 patch 3 build-fix](https://lore.kernel.org/netdev/20260515095336.92237-1-lukasz@raczylo.com/T/)                                                                                                                |
| `0004-PCI-prevent-shrink-bridge-window.patch`                               | PCI: prevent `adjust_bridge_window()` from shrinking a bridge window below the size required by `pbus_size_mem()` — fixes large-BAR / eGPU resource starvation                                                | Merged to mainline v6.19, candidate for 6.18.y stable backport | [lore patch](https://patch.msgid.link/20260219153951.68869-1-ilpo.jarvinen@linux.intel.com)                                                                                                            |
| `0005-slab-backport-flex-allocator-helpers.patch` | Incomplete backport to 6.18.x breaking the DRBD build | Cherry-picked from mainline, drop when upgrading ||
| `0006-mm-page_table_check-do-not-track-special-PFN-mapped-PTEs.patch` | mm/page_table_check: do not track special (PFN-mapped) PTEs | Linux 7.0 is not affected, but 6.18.x. is | [submission](https://lore.kernel.org/linux-mm/20260608155758.1220420-1-andrey.smirnov@siderolabs.com/T#u) |
| `../patches.disabled/minimize/0006-arm64-dts-rockchip-align-Rock-Pi-4-PCIe-with-working-Armbian.patch` | arm64: dts: rockchip: align Rock Pi 4 PCIe supplies/timing with the known-good 6.1 RockPi path: `vcc3v3_pcie` min/max voltage, `vpcie12v-supply = <&vcc12v_dcin>`, and `bus-scan-delay-ms = <1500>`. Disabled for current full-eMMC-flash minimization candidate `v1.13.4-rockpi-pcie-min-only0012-1`. | Disabled minimization candidate; Armbian-derived | Armbian 6.1 RockPi PCIe DTS behavior |
| `../patches.disabled/minimize/0007-PCI-rockchip-add-bus-scan-delay-ms-support.patch` | PCI: rockchip: add `bus-scan-delay-ms` support so board DTS can delay PCI enumeration after link-up. Confirmed removable in `v1.13.4-rockpi-pcie-min-only0012-plus-dt-1`; SATA still works without the post-link delay. | Disabled; confirmed removable | Armbian 6.1 Rockchip PCIe host behavior |
| `../patches.disabled/minimize/0008-PCI-rockchip-preserve-RK3399-reset-sequencing.patch` | PCI: rockchip: preserve historical RK3399 reset sequencing inside the newer reset-bulk implementation: PM assert/deassert order and core assert order; keep bulk core deassert because it already matches 6.1. Confirmed removable in `v1.13.4-rockpi-pcie-min-no0010-no0009-no0008-1`. | Disabled; confirmed removable | Linux 6.1 RK3399 PCIe reset sequencing |
| `../patches.disabled/minimize/0009-phy-rockchip-pcie-restore-RK3399-6.1-PHY-behavior.patch` | phy: rockchip-pcie: restore the working 6.1 RK3399 PHY shared-PHY lane de-idle order plus the legacy `PHY_CFG_WR_DISABLE == PHY_CFG_WR_ENABLE` TEST_WRITE strobe behavior. Tested in `v1.13.4-rockpi-pcie-phy61-strobe1`; not sufficient by itself. Confirmed removable in `v1.13.4-rockpi-pcie-min-no0010-no0009-1`. | Disabled; confirmed removable | Linux 6.1 RK3399 PCIe PHY behavior; upstream commits `c3fe7071e196` and `25facbabc3fc` changed this area |
| `../patches.disabled/minimize/0010-phy-rockchip-pcie-restore-RK3399-6.1-refclk-lifecycle.patch` | phy: rockchip-pcie: restore the working 6.1 RK3399 PCIe PHY `refclk` lifecycle: get the clock in probe, enable it during PHY init, and disable it during PHY exit instead of using probe-lifetime `devm_clk_get_enabled()`. Tested in `v1.13.4-rockpi-pcie-phy61-refclk1`; not sufficient by itself. Confirmed removable in `v1.13.4-rockpi-pcie-min-no0010-1`. | Disabled; confirmed removable | Linux 6.1 RK3399 PCIe PHY refclk lifecycle |
| `../patches.disabled/minimize/0011-PCI-rockchip-restore-RK3399-6.8-PERST-GPIO-initial-state.patch` | PCI: rockchip: restore the working 6.8 RK3399 initial PERST GPIO state (`GPIOD_OUT_HIGH`) before the host init reset pulse; keeps newer `GPIOD_OUT_LOW` behavior for non-RK3399 controllers. Tested in `v1.13.4-rockpi-pcie-perst-init-high1`; not sufficient by itself. Confirmed removable in `v1.13.4-rockpi-pcie-min-no0010-no0009-no0008-no0011-1`. | Disabled; confirmed removable | Armbian 6.8.11 works while 6.12.x fails; upstream 6.8→6.12 changed this initial PERST state |
| `0012-PCI-rockchip-restore-RK3399-6.8-PERST-timing.patch` | PCI: rockchip: restore the working 6.8 RK3399 PERST/link-training timing by skipping the newer 100 ms pre-PERST and 100 ms post-PERST waits only on RK3399; non-RK3399 keeps the newer timing. Tested in `v1.13.4-rockpi-pcie-perst-timing68-1`; PCIe/AHCI/SATA works. | Local RockPi compatibility patch; confirmed fix in current stack | Armbian 6.8.11 works while 6.12.x fails; upstream 6.8→6.12 added these PERST timing waits |
